<header>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">
    <% if user_signed_in? %>
    <%= current_user.name %>
    <% end %>
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-item nav-link active" href="news/show">Timer<span class="sr-only">(current)</span></a>
      <a class="nav-item nav-link" href="#">Features</a>
      <a class="nav-item nav-link" href="#">Pricing</a>
      <% unless user_signed_in? %>
        <a class="nav-item nav-link disabled"><%= link_to "ログイン", new_user_session_path, class:"login" %></a>
      <% else %>
        <a class="nav-item nav-link disabled"><%= link_to "ログアウト", destroy_user_session_path, method: :delete, class:"log-out"%></a>
      <% end %>
    </div>
  </div>
  </nav>
</header>
